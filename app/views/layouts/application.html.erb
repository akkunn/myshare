<!DOCTYPE html>
<html>
  <head>
    <title>Poteshare</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%# allの後ろにあった , 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <div class="header-left">
        <a href="/" class="headerlogo"><img src="https://presite-potepancamp-rails-02.herokuapp.com/assets/logo.png" alt="ヘッダーロゴ"></a>
        <%= form_with url: rooms_result_path, class: "header-input" do |f| %>
          <%= f.text_field :address, placeholder: "エリア" %>
        <% end %>

        <%= form_with url: rooms_search_path, class: "header-input" do |f| %>
          <%= f.search_field :keywords, placeholder: "キーワード" %> 
        <% end %>
          
      </div>
      <div class="change-buttons">
        <% if user_signed_in? %>
          <div>
            <p class="user-name"><%= current_user.name %></p>
          </div>
          <% if current_user.imr_name? %>
            <p class="header-img" id="user-img"><%= image_tag current_user.imr_name.url, :size => '70x70', class: 'user-img' %></p>
          <% else %>
            <p class="header-img" id="user-img"><%= image_tag "/assets/default_icon.jpeg", :size => '70x70', class: 'user-img' %></p>
          <% end %>                  
        <% else %>
          <%= link_to "ログイン", new_user_session_path, {class: "login"} %>
          <%= link_to "登録する", new_user_registration_path, {class: "register"} %>
        <% end %>
      </div>

      <% if user_signed_in? %>
        <%# <nav> %>
          <ul class="menu menu-vanish" id="menu">
            <li><%= link_to "ルーム登録", new_room_path %></li>
            <li><%= link_to "予約済みルーム一覧", reserves_path %></li>
            <li><%= link_to "登録済みルーム一覧", rooms_path %></li>
            <li><%= link_to "設定", user_path(current_user) %></li>
            <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete %></li>
          </ul>
        <%# </nav> %>
      <% end %>

    </header>

    <div class="yield">
      <%= yield %>
    </div>

    <footer>
      <p>&copy; 2020 POTEPAN.INC.</p>
    </footer>
     <div class="flash">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    </div>
   
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    
  </body>
</html>
